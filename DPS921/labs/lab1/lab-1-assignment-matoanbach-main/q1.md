# Lab 1 - Question 1 Report (Skeleton)

**Course:** DPS921 Parallel Algorithms and Programming Techniques  
**Student Name:**  
**Student Email:**  
**Student ID:**  
**Section:**  
**Date:**  

## 1. Objective

State what you are measuring and why.

- Goal: evaluate how compiler optimization levels affect runtime for the provided magnitude computation program.
- Deliverable: timing tables (avg of >= 3 runs), graphs, and a brief discussion of trends.

## 2. System Information

Fill in the hardware/software details used for your experiments.

- Machine / Model: Mac (arm64)
- CPU (model): Apple M1
- CPU cores / threads: 8 physical / 8 logical
- RAM: 8 GB
- OS + version: macOS 26.3 (Build 25D125)
- Power mode (plugged in / battery):

## 3. Compiler Information

Document the compiler(s) used.

- Primary compiler: Apple clang 21.0.0
- Compiler version output (from program): `Hello from the Clang compiler: 21.0.0 version`
- Build environment (IDE/terminal): terminal

Primary compiler details:

- Apple clang version 21.0.0 (clang-2100.1.1.101)
- Target: arm64-apple-darwin25.3.0

Optional (bonus):

- Secondary compiler:
- Secondary compiler version output:

## 4. Program Description

Briefly describe what `q1.cpp` does.

- Allocates an array of `n` doubles.
- Initializes the array.
- Computes magnitude: `sqrt(sum(a[i]^2))`.
- Measures time for:
  - allocation + initialization
  - magnitude calculation

## 5. Methodology

Explain how you measured performance.

### 5.1 Inputs

List the values of `n` you used (must be the same across optimization levels).

| n values used |
|---|
| 1,000,000; 3,000,000; 10,000,000; 30,000,000 |

### 5.2 Runs per Configuration

- Runs per (optimization level, n): 3
- Reported metric: average time across runs (ms)

### 5.3 Timing Method

- Timing source: `std::chrono::steady_clock`
- Units reported: milliseconds
- Notes (warm-up runs, background apps, etc.):
- Notes (warm-up runs, background apps, etc.): No explicit warm-up. Results may vary due to OS scheduling and background activity.

### 5.4 Build Configurations

List the optimization levels tested and the exact flags.

| Config | Optimization level | Flags used |
|---|---|---|
| Baseline | O0 | `clang++ -O0 -std=c++17 q1.cpp -o q1_O0` |
| Standard | O2 | `clang++ -O2 -std=c++17 q1.cpp -o q1_O2` |
| Aggressive | O3 | `clang++ -O3 -std=c++17 q1.cpp -o q1_O3` |

## 6. Results

Record results as averages over your repeated runs.

### 6.1 Allocation + Initialization Time (Average)

| n | O0 avg (ms) | O2 avg (ms) | O3 avg (ms) |
|---:|---:|---:|---:|
| 1,000,000 | 1.3333 | 0.0000 | 0.0000 |
| 3,000,000 | 5.0000 | 1.6667 | 1.0000 |
| 10,000,000 | 19.3333 | 6.0000 | 6.6667 |
| 30,000,000 | 58.0000 | 19.3333 | 20.0000 |

### 6.2 Magnitude Calculation Time (Average)

| n | O0 avg (ms) | O2 avg (ms) | O3 avg (ms) |
|---:|---:|---:|---:|
| 1,000,000 | 3.3333 | 0.3333 | 0.3333 |
| 3,000,000 | 10.6667 | 3.0000 | 3.0000 |
| 10,000,000 | 37.6667 | 11.0000 | 11.0000 |
| 30,000,000 | 116.3333 | 33.3333 | 33.6667 |

### 6.3 Results by Optimization Level

You can either create separate tables per optimization level or one combined table.

Combined table template (magnitude time recommended):

| n | O0 avg (ms) | O2 avg (ms) | O3 avg (ms) |
|---:|---:|---:|---:|
| 1,000,000 | 3.3333 | 0.3333 | 0.3333 |
| 3,000,000 | 10.6667 | 3.0000 | 3.0000 |
| 10,000,000 | 37.6667 | 11.0000 | 11.0000 |
| 30,000,000 | 116.3333 | 33.3333 | 33.6667 |

## 7. Visualizations

Include at least one plot.

- Plot 1: `n` vs magnitude time (one line per optimization level)
- Optional Plot 2: `n` vs allocation/init time

If attaching images, list them here:

- `figures/q1_magnitude_vs_n.png`
- `figures/q1_alloc_init_vs_n.png`

## 8. Analysis and Discussion

Explain trends and variability.

- How does runtime scale with `n`?
- How much improvement do you see from standard optimization vs no optimization?
- Does aggressive optimization help more than standard? Where?
- Any noise/variability across runs? Possible causes?

Observations from the results:

- Both allocation/initialization time and magnitude calculation time increase roughly linearly with `n`.
- Optimization has a larger impact on the magnitude loop than on allocation/initialization.
- `-O2` and `-O3` are close on this workload; most of the gain comes from enabling optimizations vs `-O0`.
- Some variability is expected across runs because the timing resolution is milliseconds and the OS may schedule other work.

## 9. Regression / Trend Fit

Fit a simple model to your data and report the result.

### 9.1 Model

- Model type (linear, log-linear, etc.): linear
- Dependent variable: magnitude calculation time (ms)
- Independent variable: n (elements)

### 9.2 Fit Results

Linear model used: `time_ms = a * n + b`

| Optimization | a (ms/element) | a (ms per 1e6 elements) | b (ms) | R^2 |
|---|---:|---:|---:|---:|
| O0 | 3.9049e-06 | 3.9049 | -0.9544 | 0.99996 |
| O2 | 1.1312e-06 | 1.1312 | -0.5263 | 0.99979 |
| O3 | 1.1432e-06 | 1.1432 | -0.5754 | 0.99985 |

### 9.3 Interpretation

- The high R^2 values indicate the magnitude runtime is approximately linear in `n` for the tested range.
- The slope `a` estimates the average time cost per processed element (lower is better).

## 10. Conclusion

Summarize the key takeaways in 3-6 bullets.

- Magnitude runtime increased approximately linearly with `n` for all optimization levels.
- `-O2`/`-O3` substantially reduced magnitude runtime vs `-O0` on this machine.
- Allocation/initialization also scaled with `n`, but optimization changes had less effect compared to the compute loop.

## Appendix A: Raw Timing Data

Raw timing data files committed:

- `q1_raw_timings.csv` (all runs)
- `q1_summary.csv` (averages)

Example table:

| Optimization | n | Run 1 (ms) | Run 2 (ms) | Run 3 (ms) | Avg (ms) |
|---|---:|---:|---:|---:|---:|
|  |  |  |  |  |  |

## Appendix B: Commands Used

Record the exact commands you ran.

### Build

```bash
# example only; replace with your actual commands
clang++ -O0 -std=c++17 q1.cpp -o q1_O0
clang++ -O2 -std=c++17 q1.cpp -o q1_O2
clang++ -O3 -std=c++17 q1.cpp -o q1_O3
```

### Run

```bash
# example only
./q1_O0 1000000
./q1_O0 3000000
./q1_O0 10000000
./q1_O0 30000000

./q1_O2 1000000
./q1_O2 3000000
./q1_O2 10000000
./q1_O2 30000000

./q1_O3 1000000
./q1_O3 3000000
./q1_O3 10000000
./q1_O3 30000000
```
